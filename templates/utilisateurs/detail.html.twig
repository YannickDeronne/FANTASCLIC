{% extends 'base.html.twig' %}

{% block title %}Espace personnel{% endblock %}

{% block body %}
<div class="mx-auto py-base" style="width: 50rem">
    <h1>Espace personnel</h1>

    <!-- Avatar -->
    <div class="text-center">
        <img class="my-4" src="{{ asset('uploads/avatars/' ~ app.user.avatar) }}" alt="image avatar"
            style="width: 200px; height: 200px; border-radius: 50%;">
        <a href={{ path('update_avatar') }} class="btn btn-warning ml-5">Modifier votre avatar</a>
    </div>

    <!-- info personnelles -->
    <div class="my-4">
        <div class="row profil">
            <div class="col-md-6">Votre pseudo : </div>
            <div class="col-md-6">{{ app.user.pseudo }}</div>
        </div>
        <div class="row profil">
            <div class="col-md-6">Votre adresse mail : </div>
            <div class="col-md-6">{{ app.user.email }}</div>
        </div>
        <div class="row profil">
            <div class="col-md-6">Votre année de naissance : </div>
            <div class="col-md-6">{{ app.user.anneeNaissance }}</div>
        </div>
    </div>

    <!-- btns gestion -->
    <div>
        <a href={{ path('update_utilisateur') }} class="btn btn-warning mr-3"> Modifier votre
            profil </a>
        <a href={{ path('update_password') }} class="btn btn-danger mr-3">Modifier votre mot de
            passe</a>
        <a href=# class="btn btn-danger">Supprimer votre compte</a>
    </div>
</div>
{% endblock %}